#include <stdio.h>
/**
 *main - A program that print a line using printf
 *Return: The main must return 0 (Success)
 */
int main(void)
{
printf("with proper grammer, but the outcome is a piece of art,");
return (0);
}
